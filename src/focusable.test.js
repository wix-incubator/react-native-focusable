import _ from 'lodash';
import React, { Component } from 'react';
import renderer from 'react-test-renderer';

describe('focusable', () => {
  let myScreen, myTextView;

  beforeEach(() => {
    //
  });

  it('just works', () => {
    //
  });
});
